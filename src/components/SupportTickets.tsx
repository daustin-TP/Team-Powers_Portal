import{FormEvent,useEffect,useState}from"react";import{AlertTriangle,CheckCircle2,Eye,Plus,X}from"lucide-react";import{supabase}from"../lib/supabase";import type{Profile}from"../types";type Area="maintenance"|"technology";type Store={id:string;name:string};type Update={id:string;note:string;status:string|null;image_path:string|null;created_at:string;profiles?:{full_name:string}|null};type Ticket={id:string;severity:string;description:string;steps_taken:string;external_ticket_number:string|null;image_path:string|null;status:string;acknowledged_at:string|null;created_at:string;stores?:{name:string}|null;profiles?:{full_name:string}|null;support_ticket_updates?:Update[]};
function ImageLink({path}:{path:string}){const[url,setUrl]=useState("");useEffect(()=>{supabase?.storage.from("ticket-images").createSignedUrl(path,900).then(({data})=>setUrl(data?.signedUrl??""))},[path]);return url?<a href={url} target="_blank" rel="noreferrer">View picture</a>:<span>Preparing picture…</span>}
export default function SupportTickets({profile,area}:{profile:Profile;area:Area}){const[tickets,setTickets]=useState<Ticket[]>([]),[stores,setStores]=useState<Store[]>([]),[canRespond,setCanRespond]=useState(profile.role==="admin"),[showForm,setShowForm]=useState(false),[selected,setSelected]=useState<Ticket|null>(null),[message,setMessage]=useState(""),[storeId,setStoreId]=useState(""),[severity,setSeverity]=useState("green"),[description,setDescription]=useState(""),[steps,setSteps]=useState(""),[number,setNumber]=useState(""),[file,setFile]=useState<File|null>(null),[updateNote,setUpdateNote]=useState(""),[updateFile,setUpdateFile]=useState<File|null>(null);
const load=async()=>{if(!supabase)return;const[t,s,c]=await Promise.all([supabase.from("support_tickets").select("id,severity,description,steps_taken,external_ticket_number,image_path,status,acknowledged_at,created_at,stores(name),profiles!support_tickets_submitted_by_fkey(full_name),support_ticket_updates(id,note,status,image_path,created_at,profiles!support_ticket_updates_author_id_fkey(full_name))").eq("area",area).order("created_at",{ascending:false}),supabase.from("stores").select("id,name").eq("active",true).order("name"),supabase.from("profile_capabilities").select("capability").eq("profile_id",profile.id).eq("capability",area)]);setTickets((t.data??[])as any);setStores(s.data??[]);setCanRespond(profile.role==="admin"||(c.data?.length??0)>0);if(!storeId&&s.data?.length)setStoreId(s.data[0].id)};useEffect(()=>{load()},[area]);const upload=async(f:File)=>{const path=`${profile.id}/${crypto.randomUUID()}-${f.name.replace(/[^a-z0-9.]/gi,"-")}`;const{error}=await supabase!.storage.from("ticket-images").upload(path,f);if(error)throw error;return path};
const submit=async(e:FormEvent)=>{e.preventDefault();try{const image_path=file?await upload(file):null;const{error}=await supabase!.from("support_tickets").insert({area,submitted_by:profile.id,store_id:storeId,severity,description,steps_taken:steps,external_ticket_number:number||null,image_path});if(error)throw error;setShowForm(false);setDescription("");setSteps("");setNumber("");setFile(null);setMessage("Request submitted.");load()}catch(e:any){setMessage(e.message)}};const update=async(status:string)=>{if(!selected)return;try{const now=new Date().toISOString(),image_path=updateFile?await upload(updateFile):null,patch:any={status,updated_at:now};if(status==="viewed"&&!selected.acknowledged_at){patch.acknowledged_at=now;patch.acknowledged_by=profile.id}if(status==="completed")patch.completed_at=now;const{error}=await supabase!.from("support_tickets").update(patch).eq("id",selected.id);if(error)throw error;const{error:ue}=await supabase!.from("support_ticket_updates").insert({ticket_id:selected.id,author_id:profile.id,status,note:updateNote,image_path});if(ue)throw ue;setSelected(null);setUpdateNote("");setUpdateFile(null);setMessage("Ticket updated.");load()}catch(e:any){setMessage(e.message)}};const title=area==="maintenance"?"Maintenance":"Technology";
return <div className="page"><div className="page-heading heading-with-action"><div><p className="eyebrow">Store support</p><h1>{title} requests.</h1><p>Submit a problem and follow its progress through completion.</p></div><button className="button primary" onClick={()=>setShowForm(true)}><Plus/> New request</button></div>{message&&<p className="inline-message">{message}</p>}<div className="ticket-grid">{tickets.map(t=><button className={`ticket-card severity-${t.severity}`} key={t.id} onClick={()=>setSelected(t)}><span className="severity-dot"/><div><small>{t.stores?.name} · {new Date(t.created_at).toLocaleString()}</small><strong>{t.description}</strong><span>Submitted by {t.profiles?.full_name}</span></div><span className="ticket-status">{t.status.replaceAll("_"," ")}</span></button>)}</div>
{showForm&&<div className="modal-backdrop" onClick={()=>setShowForm(false)}><form className="ticket-modal" onSubmit={submit} onClick={e=>e.stopPropagation()}><button type="button" className="icon-button modal-close" onClick={()=>setShowForm(false)}><X/></button><h2>New {title.toLowerCase()} request</h2><label>Store<select required value={storeId} onChange={e=>setStoreId(e.target.value)}>{stores.map(s=><option value={s.id} key={s.id}>{s.name}</option>)}</select></label><label>Severity<select value={severity} onChange={e=>setSeverity(e.target.value)}><option value="green">Green — Mild inconvenience</option><option value="yellow">Yellow — Operational with workaround</option><option value="red">Red — Store is not operational</option></select></label><label>Problem description<textarea required rows={4} value={description} onChange={e=>setDescription(e.target.value)}/></label><label>Steps already taken<textarea rows={3} value={steps} onChange={e=>setSteps(e.target.value)}/></label><label>Related ticket number<input value={number} onChange={e=>setNumber(e.target.value)}/></label><label>Picture<input type="file" accept="image/*" onChange={e=>setFile(e.target.files?.[0]??null)}/></label><button className="button primary full">Submit request</button></form></div>}
{selected&&<div className="modal-backdrop" onClick={()=>setSelected(null)}><section className="ticket-modal ticket-detail" onClick={e=>e.stopPropagation()}><button className="icon-button modal-close" onClick={()=>setSelected(null)}><X/></button><p className="eyebrow">{selected.stores?.name} · {selected.severity}</p><h2>{selected.description}</h2><dl><div><dt>Status</dt><dd>{selected.status.replaceAll("_"," ")}</dd></div><div><dt>Acknowledged</dt><dd>{selected.acknowledged_at?new Date(selected.acknowledged_at).toLocaleString():"Not yet"}</dd></div><div><dt>Steps taken</dt><dd>{selected.steps_taken||"None"}</dd></div><div><dt>Ticket number</dt><dd>{selected.external_ticket_number||"None"}</dd></div></dl>{selected.image_path&&<ImageLink path={selected.image_path}/>}<h3>Progress notes</h3>{selected.support_ticket_updates?.map(u=><div className="ticket-update" key={u.id}><strong>{u.status?.replaceAll("_"," ")}</strong><p>{u.note||"No note"}</p>{u.image_path&&<ImageLink path={u.image_path}/>}<small>{u.profiles?.full_name} · {new Date(u.created_at).toLocaleString()}</small></div>)}{canRespond&&<div className="responder-box"><h3>Responder update</h3><label>Notes<textarea rows={3} value={updateNote} onChange={e=>setUpdateNote(e.target.value)}/></label><label>Picture<input type="file" accept="image/*" onChange={e=>setUpdateFile(e.target.files?.[0]??null)}/></label><div className="button-row"><button className="button secondary" onClick={()=>update("viewed")}><Eye/> Viewed</button><button className="button secondary" onClick={()=>update("in_progress")}><AlertTriangle/> In progress</button><button className="button primary" onClick={()=>update("completed")}><CheckCircle2/> Completed</button></div></div>}</section></div>}</div>}
