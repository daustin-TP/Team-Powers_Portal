# Team Powers Portal v7

1. Run `supabase/migrations/202608180007_receipt_reviews_supervisor.sql` once in Supabase SQL Editor.
2. Upload the package contents to the GitHub repository root and commit to `main`.
3. Confirm the Cloudflare deployment succeeds.

Test receipt acknowledgment/approval/rejection and notes, add the same uniform item in multiple sizes to one cart, and change a non-owner user's primary role in Team Access.
